{
  "regexManagers": [
    {
      "fileMatch": ["^managed-containers/clamav-blobavscan/Dockerfile$"],
      "matchStrings": [
        "# Pinned to the amd64 build of ubuntu:24\\.04.*\\nFROM ubuntu:24\\.04@(?<currentDigest>sha256:[a-f0-9]{64})"
      ],
      "datasourceTemplate": "docker",
      "lookupNameTemplate": "library/ubuntu",
      "depNameTemplate": "ubuntu",
      "extractVersionTemplate": "24.04",
      "versioningTemplate": "docker",
      "registryUrlTemplate": "https://index.docker.io"
    }
  ]
}